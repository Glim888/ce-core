/// @func ce_hours_to_ms(hours)
/// @param {real} hours Hours to be converted to milliseconds.
/// @return {real} Hours converted to milliseconds.
gml_pragma("forceinline");
return (argument0 * 3600000);