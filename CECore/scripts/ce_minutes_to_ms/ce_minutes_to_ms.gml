/// @func ce_minutes_to_ms(minutes)
/// @param {real} minutes Minutes to be converted to milliseconds.
/// @return {real} Minutes converted to milliseconds.
gml_pragma("forceinline");
return (argument0 * 60000);